<?php
if(!defined('ABSPATH')){exit(1);}$base=get_option('cvt_integration_fixture');$make=static function(string $operation,string $delivery,string $wc='processing')use($base):int{$o=wc_create_order();$o->set_status($wc);$o->update_meta_data('_cvd_fulfillment_type','delivery');$o->update_meta_data('_cvd_operation_status',$operation);$o->update_meta_data('_cvd_delivery_status',$delivery);$o->update_meta_data('_cvd_messenger_user_id',absint($base['messenger_id']));$o->update_meta_data('_cvd_messenger_earning_status','pending');$o->update_meta_data('_cvd_owner_user_id',absint($base['gestora_id']));$o->update_meta_data('_cvd_owner_type','gestora');$o->update_meta_data('_cvd_commission_status','pending');$o->save();return $o->get_id();};
$incident=$make('with_courier','handed_over');wp_set_current_user(absint($base['messenger_id']));$open=CVD_Order_Transition_Service::open_incident($incident,'delivery',array('actor_user_id'=>absint($base['messenger_id']),'note'=>'Incidencia sintética','idempotency_key'=>'exceptions-incident-open'));if(empty($open['success'])){throw new RuntimeException('No abrió incidencia');}$resolve=CVD_Order_Transition_Service::resolve_incident($incident,'delivery',array('actor_user_id'=>absint($base['messenger_id']),'idempotency_key'=>'exceptions-incident-resolve'));if(empty($resolve['success'])){throw new RuntimeException('No resolvió incidencia');}
$legacy=$make('with_courier','incident');$o=wc_get_order($legacy);$o->update_meta_data('_cvd_delivery_history',array(array('from'=>'accepted','to'=>'to_store','actor_user_id'=>0,'at'=>'2025-01-01 00:00:00','data'=>array())));$o->save();wp_set_current_user(absint($base['admin_id']));$truncated=CVD_Order_Transition_Service::resolve_incident($legacy,'delivery',array('actor_user_id'=>absint($base['admin_id'])));if(CVD_Order_Transition_Service::CONFLICT!==$truncated['error_code']){throw new RuntimeException('Historial truncado inventó etapa');}
$before=$make('preparing','unassigned');$offered=$make('ready','offered');$assigned=$make('ready','assigned');$refund=$make('ready','offered');$failed=$make('new','unassigned');$race=$make('ready','offered');
foreach(array($before=>'cancelled',$offered=>'cancelled',$assigned=>'cancelled',$refund=>'refunded',$failed=>'failed')as$id=>$status){$o=wc_get_order($id);$o->update_status($status,'Prueba sintética 1C.4');}
update_option('cvt_exceptions_fixture',array('incident'=>$incident,'legacy'=>$legacy,'before'=>$before,'offered'=>$offered,'assigned'=>$assigned,'refund'=>$refund,'failed'=>$failed,'race'=>$race,'admin_id'=>$base['admin_id']));echo wp_json_encode(get_option('cvt_exceptions_fixture')).PHP_EOL;
